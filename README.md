# HTTP-Server-Python
Build a Basic HTTP Server from Scratch in Python  A lightweight project that implements a simple HTTP web server from scratch, using only Python’s built-in socket and threading modules — no frameworks like Flask or Django.  
